# ──────────────────────────────────────────────────────────────
# ENVIRONMENT (pick one)
# ──────────────────────────────────────────────────────────────
#   stage      →  US Staging       (default)
#   eu-stage   →  EU Staging
#   prod       →  US Production
#   eu-prod    →  EU Production
TEST_ENV=stage

# ──────────────────────────────────────────────────────────────
# TEST MODE
# ──────────────────────────────────────────────────────────────
#   local   →  Local browser (default)
#   remote  →  LambdaTest remote grid via CDP
TEST_MODE=local

# ──────────────────────────────────────────────────────────────
# LAMBDATEST GRID PROFILES (only used when TEST_MODE=remote)
# ──────────────────────────────────────────────────────────────
#   LT_PROFILE — Browser/OS to run on:
#     chrome-win    →  Chrome latest / Windows 11 (default)
#     edge-win      →  Edge latest / Windows 11
#     chrome-mac    →  Chrome latest / macOS Sonoma
#     firefox-win   →  Firefox / Windows 11
#
#   LT_RUN_PROFILE — Capture settings:
#     smoke         →  No video/network capture (fast)
#     regression    →  Full video + network capture (default)
#     debug         →  Full capture + long idle timeout
LT_PROFILE=chrome-win
LT_RUN_PROFILE=regression

# ──────────────────────────────────────────────────────────────
# LAMBDATEST CREDENTIALS
# ──────────────────────────────────────────────────────────────
LT_USERNAME=
LT_ACCESS_KEY=your_access_key

# ──────────────────────────────────────────────────────────────
# AUTH
# ──────────────────────────────────────────────────────────────
AUTH_EMAIL=your_email@example.com
AUTH_PASSWORD=your_password
AUTH_TOKEN=your_auth_token

# ──────────────────────────────────────────────────────────────
# JIRA INTEGRATION (optional — only needed for Jira tests)
# ──────────────────────────────────────────────────────────────
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_API_VERSION=/rest/api/3
JIRA_EMAIL=your_email@example.com
JIRA_API_TOKEN=your_jira_api_token
JIRA_PROJECT_KEY=LAT

# ──────────────────────────────────────────────────────────────
# REPORT-LAB (optional — enables report-lab dashboard integration)
# ──────────────────────────────────────────────────────────────
REPORT_LAB_ENABLED=false
REPORT_LAB_URL=https://qa-report.lambdatestinternal.com

# ──────────────────────────────────────────────────────────────
# SLACK (optional — Bot Token for notifications)
# ──────────────────────────────────────────────────────────────
SLACK_BOT_TOKEN=
SLACK_CHANNEL=
SLACK_MENTIONS=

# ──────────────────────────────────────────────────────────────
# URL OVERRIDES (optional — auto-resolved from TEST_ENV)
# Uncomment only if you need a custom endpoint
# ──────────────────────────────────────────────────────────────
# BASE_URL=
# TMS_BASE_URL=
# TMS_API_URL=
# AUTH_URL=
